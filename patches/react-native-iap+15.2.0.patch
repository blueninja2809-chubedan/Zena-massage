diff --git a/node_modules/react-native-iap/NitroIap.podspec b/node_modules/react-native-iap/NitroIap.podspec
index e82bb1d..3e22615 100644
--- a/node_modules/react-native-iap/NitroIap.podspec
+++ b/node_modules/react-native-iap/NitroIap.podspec
@@ -24,7 +24,7 @@ Pod::Spec.new do |s|
   # Enforce this at the podspec level so projects with a lower deployment target
   # get a clear CocoaPods error instead of a vague SwiftCompile failure.
   # Platform versions match OpenIAP requirements: iOS 15+, macOS 14+, tvOS 16+, watchOS 8+
-  s.platforms    = { :ios => '15.0', :tvos => '16.0', :macos => '14.0', :watchos => '8.0', :visionos => 1.0 }
+  s.platforms    = { :ios => '15.0', :tvos => '16.0', :macos => '14.0', :watchos => '8.0', :visionos => '1.0' }
   s.source       = { :path => '.' }
 
   s.source_files = [
diff --git a/node_modules/react-native-iap/lib/module/index.js b/node_modules/react-native-iap/lib/module/index.js
index 011634f..d7abe22 100644
--- a/node_modules/react-native-iap/lib/module/index.js
+++ b/node_modules/react-native-iap/lib/module/index.js
@@ -417,6 +417,7 @@ export const developerProvidedBillingListenerAndroid = listener => {
 
 const subscriptionBillingIssueJsListeners = new Set();
 let subscriptionBillingIssueNativeAttached = false;
+let subscriptionBillingIssueNativeUnavailable = false;
 const subscriptionBillingIssueNativeHandler = nitroPurchase => {
   if (!validateNitroPurchase(nitroPurchase)) {
     RnIapConsole.warn('[subscriptionBillingIssueListener] dropped malformed native payload');
@@ -432,9 +433,15 @@ const subscriptionBillingIssueNativeHandler = nitroPurchase => {
   }
 };
 function tryAttachSubscriptionBillingIssueNative() {
-  if (subscriptionBillingIssueNativeAttached) return;
+  if (subscriptionBillingIssueNativeAttached || subscriptionBillingIssueNativeUnavailable) return;
+  const addBillingIssue = IAP.instance?.addSubscriptionBillingIssueListener;
+  if (typeof addBillingIssue !== 'function') {
+    subscriptionBillingIssueNativeUnavailable = true;
+    RnIapConsole.warn('[subscriptionBillingIssueListener] addSubscriptionBillingIssueListener is missing on IAP.instance (rebuild iOS/Android after upgrading react-native-iap). Billing-issue callbacks will be skipped.');
+    return;
+  }
   try {
-    IAP.instance.addSubscriptionBillingIssueListener(subscriptionBillingIssueNativeHandler);
+    addBillingIssue.call(IAP.instance, subscriptionBillingIssueNativeHandler);
     subscriptionBillingIssueNativeAttached = true;
   } catch (e) {
     const msg = toErrorMessage(e);
